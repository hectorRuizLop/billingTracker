"use strict";

/**
 * Reactiva un cliente que había sido marcado como eliminado.
 * @param {import('@sap/cds').Request} _req - Objeto de petición CAP con parámetro clientId
 * @returns {string} Mensaje de confirmación
 */
async function reactivateClient(_req) {
  // TODO: implement
}

module.exports = { reactivateClient };
